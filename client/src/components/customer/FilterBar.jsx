import { Box } from '@mui/material';
import React from 'react';

const FilterBar = ({onFilterChange}) => {

    
    return (
        <Box className='filter-bar' display='flex' width='100%' bgcolor='blue'>

        </Box>
    );
};

export default FilterBar;